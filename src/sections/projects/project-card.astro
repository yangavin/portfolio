---
type Props = {
  project: {
    logo: string;
    techUsed: {
      logo: string;
      title: string;
      url: string;
    }[];
    title: string;
    projectUrl: string;
    description: string;
    repoUrl: string;
    color: string;
  };
};
const {
  project: { logo, techUsed, title, projectUrl, description, repoUrl, color },
} = Astro.props;
---

<div class="container p-4">
  <h1>{title}</h1>

  <div>
    <a href={projectUrl} title={`Launch ${title}`}>
      <img src="goto.svg" alt="Go-to-link image" srcset="" />
    </a>
  </div>

  <div>
    <p>{description}</p>
  </div>

  <div><img src={logo} alt="Project logo" srcset="" class="w-1/3" /></div>

  <div>
    <h2 class="mb-2">Built with:</h2>
    <div class="flex">
      {
        techUsed.map((tech) => (
          <a href={tech.url} title={tech.title}>
            <img src={tech.logo} alt={tech.title} srcset="" />
          </a>
        ))
      }
    </div>
  </div>

  <div>
    <a href={repoUrl} title={`${title} repository`}>
      <img src="github.svg" alt="GitHub Logo" srcset="" />
    </a>
  </div>
</div>

<style>
  .container {
    border-radius: 0.4375rem;
    background: linear-gradient(
      119deg,
      #a383fc 1.7%,
      rgba(221, 208, 255, 0.6) 58.67%
    );
    display: grid;
    grid-template-columns: 60% 25%;
    column-gap: 15%;
    grid-template-rows: 3rem 1fr 3rem;
  }
</style>
